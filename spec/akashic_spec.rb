# frozen_string_literal: true

RSpec.describe Akashic do
  it "has a version number" do
    expect(Akashic::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
